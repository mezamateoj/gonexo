{
  "name": "@gonexo/back",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "lint": "oxlint .",
    "generate-types": "wrangler types --env-interface CloudflareBindings",
    "typecheck": "pnpm run generate-types && tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.25"
  }
}
